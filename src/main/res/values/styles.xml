<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar.StatusBarColored" parent="AppTheme.NoActionBar" />

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="fade_animation">
        <item name="@android:windowEnterAnimation">@anim/fade_out</item>
        <item name="@android:windowExitAnimation">@anim/fade_in</item>
    </style>

    <style name="TextAppearance.PhotoGallery" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/font_photo_gallery</item>
        <item name="android:background">@drawable/text_photo_gallery_background</item>
        <item name="android:maxLines">15</item>
        <item name="android:padding">@dimen/spacing_small</item>
    </style>
</resources>
